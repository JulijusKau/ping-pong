import {
  StyledContainerDiv,
  StyledLoadingSpinner,
} from "./StyledLoadingSpinner";

export const LoadingSpinner = () => {
  return (
    <StyledContainerDiv>
      <StyledLoadingSpinner />
    </StyledContainerDiv>
  );
};
